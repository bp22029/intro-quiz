# イントロクイズ 当日運用手順書

本番URL: `https://<デプロイ先のホスト>`（以下この手順書では **BASE** と表記します）

| 画面 | URL | 役割 |
|---|---|---|
| 参加者 | `BASE/` | スマホで名前登録・早押し |
| 投影 | `BASE/screen` | プロジェクターに表示。操作しない |
| 管理 | `BASE/host` | 手元PCで進行をクリック操作 |

キーボード操作はありません。進行操作はすべて手元PCの `/host` で行います。

## 開演前の準備

### 1. Renderを起こす — 出番の20分前

1. 本番URLを開く。
2. スリープからの復帰中は表示まで30秒〜1分ほど待ち、必要なら再読み込みする。
3. 参加者画面が表示されたら、続けて `/screen` と `/host` も開く。
4. 以後は本番終了まで画面を開いたままにする。

### 2. 投影PCを準備する

1. 投影PCをプロジェクターと会場の音声出力へ接続する。
2. Chromeで 投影画面 `BASE/screen` を開く。
3. 「投影を開始する」をクリックする。確認のビープが鳴ることを確かめる。このクリックをしないと、早押し時のビープやYouTube音声がブラウザに止められることがある。
4. プロジェクターにQRコード、問題番号、参加者欄が見えていることを確認する。
5. YouTubeモードを使う場合、曲の音は投影画面側から出る。投影PCの音声出力先と音量を確認する。

`/screen` は表示専用です。投影画面でキーを押しても進行操作はできません。

### 3. 手元PCの管理画面を準備する

1. Chromeで 管理画面 `BASE/host` を開く。
2. 「操作をはじめる」をクリックする。確認のビープが鳴ることを確かめる。
3. 左上が「● 接続中」になっていることを確認する。
4. 再生モードが既定の「手動」になっていることを確認する。
5. 手動モードでは、管理画面を画面の左側、YouTubeを右側または別ウィンドウに置くと操作しやすい。
6. 手動モードの曲は手元PCから鳴るため、手元PCも会場音響へ出力できることを確認する。

### 4. 開演前テストをする

1. スマホ1台で投影画面のQRコードを読み、名前を入れて参加する。
2. 早押しボタンを押す。
3. `/screen` と `/host` に回答者名が表示され、両方でビープが鳴ることを確認する。
4. `/host` の「押し直し」をクリックし、ロックせず受付中へ戻ることを確認する。
5. もう一度押し、「× お手つき」で不正解表示と `3 → 2 → 1 → GO!` のカウントダウンを確認する。
6. 「この問題をやり直す」をクリックして、試験で付いた押下・答え表示・お手つきロックをすべて消す。
7. 参加予定のスマホがQRから接続できることと、管理画面の参加人数が増えることを確認する。

## 再生モード

管理画面右上のトグルで「手動」と「YouTube」を切り替えます。既定は、トラブルに強い **手動モード** です。

### 手動モード（既定・推奨の逃げ道）

- 事前に「再生タブを用意」をクリックして、曲再生用のタブを開いておく。
- 管理画面の「▶ イントロ (N秒〜)」をクリックすると、そのタブが `startSec` から再生する。2回目以降はタブが切り替わらず、管理画面からフォーカスが外れない。
- **早押しのビープを聞いたら、操作者が再生タブへ移って手で止める。** 手動モードで人の手が要るのはここだけ。
- 「○ 正解・答えを出す」を押すと、「サビを鳴らすまで」に設定した秒数の後、同じタブが `chorusSec` から再生する（自動サビ再生をオフにもできる）。
- 手動でサビへ飛ばす場合は「♪ サビ (N秒〜)」をクリックする。
- **問題を移ると（「前の問題」「次の問題」「問題一覧のクリック」「この問題をやり直す」）、再生タブは自動で無音になる。** 正解発表で鳴らしたサビを手で止める必要はない。タブ自体は残るので、次の曲もそのまま使い回せる。
- 答え表示やお手つき判定は、通常どおり管理画面のボタンで行う。

### YouTubeモード

- 曲の再生は投影画面内のYouTubeプレイヤーが担当する。映像は表示されない。
- 管理画面の「▶ イントロ再生」と「⏸ 一時停止」で操作する。
- 参加者が押すと自動で一時停止する。
- 「○ 正解・答えを出す」を押すと、`chorusSec` へジャンプしてサビを自動再生する。
- モードを切り替えた直後は全曲のプレイヤー準備に少し時間がかかることがある。本番前に全曲を試す。

## 1問の進行手順

1. **問題を選ぶ。** 管理画面の「前の問題」「次の問題」または画面下の問題一覧をクリックする。問題を選ぶと、前の押下・答え表示・お手つきロックは解除される。
2. **イントロを再生する。**
   - 手動モード: 外部のYouTubeを `startSec` の位置から手で再生する。
   - YouTubeモード: 「▶ イントロ再生」をクリックする。
3. **早押しを待つ。** 最初の1人だけが受理される。ビープが鳴り、投影画面と管理画面に回答者名が出る。YouTubeモードでは自動停止する。手動モードではビープを合図に外部YouTubeを手で止める。
4. **回答を判定する。**
   - 正解: 「○ 正解・答えを出す」をクリックする。投影画面に曲名・アーティスト・推した人・正解者が表示される。`chorusSec` からサビが自動再生される（手動モードは再生タブ、YouTubeモードは投影画面）。手動でサビへ飛ばす場合は「♪ サビ (N秒〜)」をクリックする。
   - お手つき: 「× お手つき」をクリックする。回答者はその問題だけロックされ、全画面に不正解と3秒カウントダウンが出る。`GO!` が出たら他の参加者の受付が再開する。曲も続ける場合は、手動モードなら外部YouTubeを手で再開し、YouTubeモードなら「▶ イントロ再生」をクリックする。
   - 誤検知・押し間違い: 「押し直し」をクリックする。回答者をロックせず、すぐ受付中へ戻る。
5. **次の問題へ進む。** 答え合わせが終わったら「次の問題」をクリックする。全員のお手つきロックと表示が消え、次の問題へ移る。手動モードでは、鳴っていたサビもここで自動的に止まる。

同じ問題を最初からやり直す場合は「この問題をやり直す」をクリックします。現在の問題番号は変えず、押下・答え表示・全員のお手つきロックを解除します。

## 参加者とお手つきの扱い

- 参加者はスマホのブラウザに保存された `clientId` で識別される。
- お手つきした参加者がページを再読み込みしたり名前を変えたりしても、その問題のロックは解除されない。
- 同じ表示名で別ブラウザから入り直した場合も、その問題ではロックされる。
- ロックは「次の問題」「前の問題」、問題一覧での選択、または「この問題をやり直す」で解除される。
- 管理画面の参加者一覧では、名前をクリックして個別のお手つきロックを付け外しできる。別ブラウザからの入り直しなどを司会が手動で処理するときに使う。
- スマホが一時的に切れた場合は同じページを開き直す。保存済みの名前で自動復帰する。

## `songs.json` の書き方

問題データは `public/songs.json` にJSON配列で記述します。コメントや末尾の余分なカンマは入れません。

```json
[
  {
    "videoId": "dQw4w9WgXcQ",
    "title": "曲名",
    "artist": "アーティスト名",
    "owner": "曲を推した人の名前",
    "startSec": 0,
    "chorusSec": 43
  }
]
```

| 項目 | 内容 |
|---|---|
| `videoId` | YouTube URLの `v=` より後ろにある動画ID |
| `title` | 答えとして投影する曲名 |
| `artist` | 答えとして投影するアーティスト名 |
| `owner` | 曲を推した人の名前。画面側で「さんの推し曲」が付く |
| `startSec` | イントロ再生を開始する位置（秒） |
| `chorusSec` | 正解時にジャンプして再生するサビの位置（秒） |

`chorusSec` を省略した場合、YouTubeモードの正解時は `startSec` から再生されます。実運用では全曲に設定してください。

### YouTube埋め込みを確認する

1. 公式チャンネルの動画を優先して選ぶ。
2. Chromeで `https://www.youtube.com/embed/<videoId>` を直接開く。
3. 実際に再生し、「動画を再生できません」や埋め込み禁止エラーが出ないことを確認する。
4. `startSec` と `chorusSec` の位置が意図した箇所か、通常のYouTube画面でも確認する。
5. 広告の有無と会場アカウントでの再生可否を確認する。
6. 埋め込み不可に備え、各曲の代替動画を用意する。
7. 変更を本番へ反映した後、管理画面をYouTubeモードへ切り替え、全曲を実際にテストする。

## トラブル時の逃げ道

### YouTubeモードで曲が再生できない・埋め込みエラーが出る

管理画面右上のトグルを **「手動」** に切り替える。「▶ イントロ (N秒〜)」で再生タブに曲を出し、以降は再生を管理画面から、停止を手で行う。早押し、ビープ、回答者表示、正解表示、お手つきは手動モードでもそのまま動く。

エラー101または150は、その動画が埋め込み再生を禁止している。代替の `videoId` へ差し替えるか、手動モードで続行する。

### 広告が入る・イントロの頭が欠ける

すぐ手動モードへ切り替え、事前に用意した動画を外部YouTubeで頭出しして使う。進行を止めて埋め込み側を直そうとしない。

### ビープが鳴らない

1. 音が鳴らない側の `/screen` または `/host` を再読み込みする。
2. `/screen` は「投影を開始する」、`/host` は「操作をはじめる」をもう一度クリックする。
3. Chromeのサイト音声、PC音量、HDMI接続後の音声出力先を確認する。
4. 直らない場合、管理画面の回答者名表示を合図に手で曲を止める。

### お手つきを誤って付けた

すぐ受付からやり直す場合は「この問題をやり直す」をクリックする。3秒演出を打ち切り、その問題の全員のロックを解除して受付中へ戻る。演出後に特定の1人だけを解除する場合は、管理画面の参加者一覧で「お手つき」と表示された名前をクリックする。

### 管理画面が「再接続中」になった

ネットワークを確認し、数秒待つ。戻らなければ `/host` を再読み込みし、「操作をはじめる」をクリックする。サーバーが再起動していなければ、問題と早押しの状態はサーバーから再取得される。

### Renderが再起動した

サーバー状態はメモリだけにあるため、問題番号・参加者・お手つき状態が初期化される。`/screen` と `/host` を再読み込みして音声解除ボタンをクリックし、参加者にはスマホ画面を開き直してもらう。管理画面で問題を選び直して再開する。

### スマホが参加できない

1. 投影PCと手元PCで本番URLが開くか確認する。
2. Renderの復帰中なら最大1分待って再読み込みする。
3. QRコードの接続先が BASE のホスト名になっていることを確認する。
4. 参加者にはQRを読み直すか、本番URLを直接入力してもらう。

## 最終チェックリスト

- [ ] Renderを出番の20分前に起こした
- [ ] `/screen` で「投影を開始する」をクリックし、ビープを確認した
- [ ] `/host` で「操作をはじめる」をクリックし、「● 接続中」を確認した
- [ ] 投影PCと手元PCの音声出力先を確認した
- [ ] QRからスマホで参加し、早押し・正解・お手つき・3秒カウントダウンを確認した
- [ ] `songs.json` の全曲についてイントロ位置・サビ位置・埋め込み再生を確認した
- [ ] 代替動画と手動モード用のYouTube画面を用意した
- [ ] テスト後に「この問題をやり直す」で状態を消し、第1問を選んだ
- [ ] 管理画面の再生モードが当日使う設定になっている
